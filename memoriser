from tkinter import *
from tkinter import messagebox
from tkinter.filedialog import *

window = Tk()
window.title("Memoriser app")
window.geometry("800x500")

def add_item():
    pass

def open_file():
    pass

def save_file():
    pass

def delete_item():
    pass

fopen = Button(window, text = "open", command = None )


fsave = Button(window, text = "Save list", command = None )

ldelete = Button (window, text= "Delete item", command = None)

ladd = Button(window, text = "add the list", command = None )

item = Entry (window, width =30)
item.place(x = 300, y = 150)


frame = Frame(window)
frame.place (x=200, y=180)
scroll = Scrollbar(frame, orient = "vertical")
listbox = Listbox(frame, width = 50 , yscrollcommand = Scrollbar.set, bg = "red")
for i in range (1,100):
    listbox.insert(END, "Item" + str(i))

listbox.pack( side = LEFT, padx =5)
scroll.pack(side = RIGHT, fill = Y)
scroll.config(command = listbox.yview)
frame.pack(side= BOTTOM)
fopen.place(x=20, y = 250)
fsave.place(x=300, y= 20)
ladd.place(x= 300, y= 100)
ldelete.place(x=650, y = 250)
window.mainloop()
